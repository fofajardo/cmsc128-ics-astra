export const metadata = {
  title: "What's Up",
  description: "Latest news, announcements, and newsletters from the community.",
};

export default function WhatsUpLayout({ children }) {
  return <>{children}</>;
}