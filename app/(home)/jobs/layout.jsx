export const metadata = {
  title: "Job Opportunities",
  description: "Browse job postings shared by ICS alumni and partners to help kickstart your career.",
};

export default function JobOpportunitiesLayout({ children }) {
  return <>{children}</>;
}