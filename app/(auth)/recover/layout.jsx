export const metadata = {
  title: "Recover Password",
  description: "Reset your ICS-ASTRA account password securely.",
};

export default function RecoverPasswordLayout({ children }) {
  return <>{children}</>;
}